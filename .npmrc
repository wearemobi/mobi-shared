# Force npm to include native bindings for multiple architectures in the lockfile
# This fixes the "MODULE_NOT_FOUND" errors for Rollup/Vite during the 'prepare' phase.
libc=glibc,musl
os=linux,darwin
cpu=x64,arm64
